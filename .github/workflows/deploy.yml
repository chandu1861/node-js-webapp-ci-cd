name: CI/CD Pipeline

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: Install dependencies
      run: npm install

    - name: Run tests
      run: npm test

    - name: Build Docker image
      run: docker build -t your-dockerhub-username/node-cicd-app .

    - name: Build and push Docker image
      run: |
         docker build -t ${{ secrets.DOCKER_USERNAME }}/node-cicd-app:latest .
         docker push ${{ secrets.DOCKER_USERNAME }}/node-cicd-app:latest

    - name: Push Docker image
      run: docker push your-dockerhub-username/node-cicd-app

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
    - name: SSH into EC2 and deploy
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.EC2_SSH_KEY }}
        script: |
          docker pull your-dockerhub-username/node-cicd-app
          docker stop app || true
          docker rm app || true
          docker run -d --name app -p 80:3000 your-dockerhub-username/node-cicd-app
